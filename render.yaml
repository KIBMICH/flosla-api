services:
  - type: web
    name: event-payment-api
    env: node
    node: 20
    buildCommand: npm ci --include=dev && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
